using UnityEngine;
using UnityEngine.UI;
using UnityEngine.UIElements;

public class SideNavScript : MonoBehaviour
{
    public DropdownMenu menu;

    void Start()
    {
        // Set the initial state of the menu
        // menu.isOpen = false;
    }

    public void OpenMenu()
    {
        // Open the menu
        // menu.isOpen = true;
    }

    public void CloseMenu()
    {
        // Close the menu
        // menu.isOpen = false;
    }
}
